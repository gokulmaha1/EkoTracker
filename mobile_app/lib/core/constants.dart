class AppConstants {
  static const String baseUrl = 'http://10.0.0.1:3000/api'; // Use 10.0.2.2 for Android emulator, but let's stick to a generic one or localhost if web. Let's use 10.0.2.2 for Android.
  // Actually, for real device testing, need local IP. For emulator 10.0.2.2.
  static const String appName = 'EkoTracker';
}
